# Copy to secrets.yaml and replace every placeholder before connecting or flashing.
wifi_ssid: "YOUR_WIFI_NETWORK"
wifi_password: "YOUR_WIFI_PASSWORD"
# Generate each API key with: openssl rand -base64 32
api_encryption_key: "YOUR_VOLTAGE_READER_BASE64_KEY"
ota_password: "YOUR_OTA_PASSWORD"
large_7_seg_api_key: "YOUR_DISPLAY_BASE64_KEY"
large_7_seg_fallback_password: "YOUR_DISPLAY_FALLBACK_PASSWORD"
